'use strict';

const assert = require('chai').assert;

describe('build(entry)', function() {
    describe('when entry is a valid source file', function() {
        it('should return valid ES5 code', function() {
            assert(false);
        });
    });
    describe('when entry is a invalid source file', function() {
        it('should throw an error', function() {
            assert(false);
        });
    });
    describe('when entry is an invalid path file', function() {
        it('should throw an error', function() {
            assert(false);
        });
    });
});